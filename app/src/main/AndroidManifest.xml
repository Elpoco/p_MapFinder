<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.elpoco.p_mapfinder">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_1"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MapActivity" android:parentActivityName=".MainActivity"/>
        <activity android:name=".MapSeaActivity" android:parentActivityName=".MainActivity"/>
        <activity android:name=".ShareActivity" android:parentActivityName=".MainActivity"/>
        <activity android:name=".MypageActivity" android:parentActivityName=".MainActivity"/>
        <activity android:name=".SettingActivity" android:parentActivityName=".MainActivity"/>
        <activity android:name=".BoardActivity" android:parentActivityName=".ShareActivity"/>
        <activity android:name=".WriteActivity" android:parentActivityName=".ShareActivity"></activity>
    </application>

</manifest>